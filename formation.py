
import logging
import numpy

from graph import graph

class formation():
	"""
	"""
	
	def __init__(self, graph):
		return
		
	# ID to table entry dictionary
	ID_lookup = {3122 : 0, 1234 : 1}
	
	def run(self, graph):
		if graph.is_complete():
			if not graph.is_directed():
				#we have an complete undirected graph
				# henneberg sequence
		return	